name: ${project.artifactId}
main: ${mainClass}
version: ${project.version}
author: ${author}
api-version: 1.15
depend: [MineSQLAPI]
commands:
  colonies:
    description: colonies base command
    aliases: [c]
  accept:
    description: Accept an invite to a Colony
  decline:
    description: Decline an invite to a Colony
